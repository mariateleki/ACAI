# ACAI

Setting up for the first time:
```
install npm 
clone the repo
```

local machine only: 
```
npm install
npm run dev
```

you only need these if you're going to deploy to github pages:
```
npm install gh-pages --save-dev
npm install gh-pages --save-dev --legacy-peer-deps
npm run deploy
```
